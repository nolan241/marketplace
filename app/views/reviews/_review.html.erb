<p>
  <%= gravatar_tag review.user.email, size: 20 %> 
  <%= review.content %> 
  by <%= review.user.name %>
  <!-- opulated by their unique score #ID -->
  <div class="score_<%= review.id %>"></div>
</p>